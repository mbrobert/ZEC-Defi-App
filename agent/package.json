{
  "name": "@zyo/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain agent: monitors Rhea health factor and LP positions, decides when to claim rewards, and executes reward routing (compound or send-to-ZEC via NEAR Intents 1-Click). Zero runtime dependencies by design.",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@zyo/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
