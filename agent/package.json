{
  "name": "@zyo/agent",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Oilskin keeper: watches every OilskinAccount's Aave v3 health on Base via viem, runs the shared HF ladder with hysteresis, and acts only through execAsKeeper within the user's on-chain grant. Fail-closed valuation, progress watchdog, crash-safe store, idempotent dispatch.",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "tsc -p tsconfig.json && node scripts/verify-abi.mjs && node --test \"dist/test/*.test.js\"",
    "test:only": "node --test \"dist/test/*.test.js\"",
    "verify-abi": "node scripts/verify-abi.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@zyo/shared": "*",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fast-check": "^4.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
