# Finding from the nightly invariant configuration — NI-HIGH-1 (2026-09-12)

**This is not an external audit.** It is the record of one finding, produced by running the
invariant suite at the depth the new nightly job uses (`.github/workflows/nightly-invariants.yml`,
slice I: `FOUNDRY_INVARIANT_RUNS=1500 FOUNDRY_INVARIANT_DEPTH=120`) locally before that job had
ever run in CI, on the tree at `c7d90f1`. Nothing was signed or broadcast, `.env` was not read, no
`acceptVenue` exists anywhere in the tree, `Deploy.s.sol` still points every asset at Aave.

Abbreviations: LP = liquidity provision; HF = health factor; PoC = proof of concept; ABI =
application binary interface.

## The count

| Scope | Critical | High | Medium | Low | Info |
|---|---|---|---|---|---|
| `StrategyRouter.unwind` — the swap leg of every Close | 0 | 1 | 0 | 0 | 0 |

## Fix round (same session)

| id | Status | What changed | Test that failed first → passes now |
|---|---|---|---|
| NI-HIGH-1 | **Fixed** (the commit that adds this file) | `StrategyRouter._toUsdc` asks the adapter for its floor (`minOutFor`) before swapping a non-USDC leg; a real quote whose floor for that leg is **zero USDC** means the swap could not be protected, so the leg is left in the account, `DustLegKept(account, token, amount)` is emitted, and the unwind carries on. A quote with no numbers is still the adapter's `ZeroQuote`, exactly as before. | `contracts/test/audit-regressions/DustLegClose.t.sol` (4: the web's single Close, the keeper's protective unwind, a priceable leg still swapped + an empty quote still refused, and a 512-run fuzz that the boundary is exactly the adapter's floor) — on `c7d90f1` three of the four fail `ZeroQuote()` and the fuzz fails on every dust case; `invariant_singleCloseClearsEveryBook` at 1500 × 120 fails on `c7d90f1` and passes with the fix (180,000 calls, 0 reverts) |

---

## NI-HIGH-1 — a fee worth less than one USDC unit reverted the whole Close, and the keeper's protection with it

**Severity.** High. Protection that cannot fire on a reachable state: the keeper's `unwind` and
the web's single Close are the same router call, and both reverted. The raw exit through `exec`
was never affected (`invariant_userCanAlwaysExitViaExec` held throughout), which is why this is
not Critical — the user could always leave by hand; the product path, and the automated
protection the product tells the user is watching their loan, could not.

**Where.** `contracts/src/router/StrategyRouter.sol`, `_toUsdc` (the swap step of `unwind`, called
once per pool token on line 398 with each leg the LP close paid out). Before the fix it swapped
every non-zero, non-USDC leg unconditionally with the caller's quote.

**The failing scenario, executed.** The nightly configuration's first local run (109 s) reported
`invariant_singleCloseClearsEveryBook`: "a two-book single Close reverted although the account was
funded to cover every book", shrunk from 68 calls to 7: `switchVenue(true)` →
`supplyAndBorrowOnCurrentVenue(1.47e8, …)` (a Morpho book) → `openLp(200000)` →
`accrueYield(…, w = 6880, …)` (6,880 wei of WETH fee accrued on the position) → `switchVenue(false)`
→ `supplyAndBorrowOnCurrentVenue(9779, 4225)` (a second, tiny Aave book) → `singleCloseProbe()`.
Replayed deterministically with full traces (the scratch test is not committed; the regression
below carries the essence): the venue's close pays the account 6,192 wei of WETH (6,880 net of the
10 % performance fee) and 31,757 USDC; the router calls
`AerodromeSwapAdapter.swap(WETH, USDC, 6192, quotedIn 1e18, quotedOut 2,453.45e6, 100 bps)`; the
adapter's floor `6192 × 2453450000 × 9900 / (1e18 × 10000)` is **0**; `swap` refuses
`ZeroQuote()`; the account's `execWithCallback` bubbles it; the Close is gone. The two books are
incidental — the same happens on a one-book account; the deep run reached the state because
`accrueYield`'s WETH bound starts at 0 and only 180,000 calls per invariant sampled a value that
small next to an open position and a funded probe.

**Why it is real on Base and not only in the mock.** The engine's close pays both pool tokens: the
principal comes back one-sided (Addendum 5: 999.999999 USDC and 0 WETH), but fees accrue in both
tokens whenever the price crosses the range, and the AERO / WETH / cbZEC legs the engine and the
direct venue hand back are whatever the pool did — tiny amounts are the ordinary case, not the
edge. Any leg below `quotedIn / (quotedOut × (1 − tolerance))` — about 4.1 × 10⁸ wei of WETH at
2,453 USDC/WETH, i.e. anything under about one USDC unit of value — reverted the unwind. The
keeper builds its quote the same way (`agent/src/dispatch/quote.ts`), so a position that had earned
a few wei of the other token became a position the ladder could not repay.

**Why the fix is the adapter's floor and not a router constant.** The adapter's `minOutFor` is
the number the chain enforces (`RISKS.md` §13); a leg whose floor is zero could not be protected by
any swap the router is allowed to send — the adapter would refuse it by name. So "dust" is defined
by the code that would have refused it, at the caller's own quote and tolerance, never by a typed
threshold. Below the floor the token stays in the account (the user's, sweepable, closable later);
at or above it the leg is swapped exactly as before. An empty quote is still refused: the caller
gave nothing to protect the swap with, and that is the adapter's decision, not this one.

**Closed the same day (the commit after the static-analysis triage).** The keeper now reads
`DustLegKept` (`agent/src/abi/oilskin.ts` fragment, seam 110 → 111; `summarizeUnwinds` sums it per
token into `dustKept`; a CONFIRMED repay carries `dustKeptNote` — "left in the account, not
swapped (below the quote's floor): N base units of <token>" — the repay figures unchanged;
`dispatcher.test.ts` +1), and the web's Close plan says it in both registers: the plain sentence
("a leftover too small for that floor to price — a few wei of the other token — stays in your
account instead of failing the close") and the technical note naming the event (`plan.test.ts`
asserts both). The direct venue's path shares `_toUsdc`, so the cbZEC / AERO legs get the same rule
with no separate change.

**ABI.** One new event on `StrategyRouter`: `DustLegKept(address indexed account,address indexed
token,uint256 amount)`; `contracts/abi/oilskin-abi.json` and `web/lib/abi/oilskin.generated.ts`
regenerated, the keeper's seam unchanged (`docs/CONTRACT-ABI.md`).

**Regression.** `contracts/test/audit-regressions/DustLegClose.t.sol`; the invariant that found it
is unchanged and now green at the nightly depth. `docs/TESTING.md` carries the counts.

---

## Static analysis — the first CI runs of the slice H job, triaged (2026-09-12)

Slither ran for the first time on the push of `c7d90f1` (and on every push since): **305 results
across 54 contracts and 102 detectors**, and the job failed on `--fail-high`. Aderyn and halmos
did not run behind it (the job stops at the first failing step). Every High is triaged below; each
is a pattern this codebase chose on purpose and already proves with a test, and each is now
suppressed at its site with a `// slither-disable-next-line` that names the proof. (The first
triage commit named the swap adapters' finding `reentrancy-eth`; Slither files it under
`reentrancy-balance`, so the run after it still failed — the suppression now names both, and the CI
command run locally exits 0 at 296 results with no High.) The Mediums
and Lows do not fail the job; they are recorded with a verdict so the next reader does not
re-triage them.

| Detector (severity) | Where | Verdict | Proof / note |
|---|---|---|---|
| `arbitrary-send-eth` (High) | `OilskinAccount._rawCall` | **By design** — the owner's own account sends the value it is told to send; the owner's doors are owner-only, a keeper's value is charged against `maxValuePerPeriod` before the call, a peripheral's against the root grant | `Account.t.sol` (owner-only `exec*`, value budget, `ValueBudgetExceeded`), `audit-regressions/GrantEscape.t.sol` |
| `reentrancy-eth` / `reentrancy-no-eth` (High / Medium) | `OilskinAccount._runRootCalls`, `OilskinAccount.execFromPeripheral` | **By design** — the "state written after the external call" is the NEXT call's budget charge inside one batch; each call's own charge precedes it; re-entry into any root door reverts `Reentrancy()` on the transient lock, and the peripheral door admits only the one active target, depth-bounded | `Account.t.sol` `test_reentrancy_*` (three tests), `audit-regressions/PeripheralCallback.t.sol` |
| `reentrancy-balance` (High), five findings — named `reentrancy-eth` in the first triage commit, which is why the second run still failed |  `AerodromeSwapAdapter.swap`, `SlipstreamPoolSwapAdapter.swap` | **By design** — "balance read before the call, used after it" IS the floor check: the account's balance delta across the swap is what the floor is enforced on (W3-LOW-6), and the adapters hold nothing between calls; the pool-direct adapter's callback accepts only the bound pool while a swap is in flight (`T_PAID`) | `audit-regressions/SwapAdapterFloor.t.sol`, `SlipstreamLpVenue.t.sol` |
| `uninitialized-state` (High) | `CollateralRegistry._previousVenues` | **False positive** — a mapping of arrays only ever `push`ed to (`_rememberPrevious`); the detector does not count `push` as a write | `audit-regressions/VenueSwitch.t.sol` reads it after a switch |
| `pyth-unchecked-confidence` (Medium) | `PythOracleAdapter._pythE8`, `peek` | **Open, for the founder** — the adapter takes Pyth's `price` and ignores `conf`; the peg is cross-checked against the pool TWAP (`PythOracleAdapter.t.sol`), which bounds a wrong price but not a wide one. A `conf / price` ceiling is a product decision on cbZEC pricing, not a bug to patch silently | `RISKS.md` §5 |
| `pyth-unchecked-publishtime` (Medium) | `PythOracleAdapter.peek` | **By design** — `peek` is the documented unsafe view (`getPriceUnsafe`); the pricing path uses `getPriceNoOlderThan(maxAge)` | `PythOracleAdapter.t.sol` (`StalePrice`, `peek`) |
| `incorrect-equality` (Medium) | `MorphoMath.expectedBorrowTotals`, `StrategyRouter._repayAcross` / `sweep`, `AaveV3Venue.repay` | **By design** — every one is an `== 0` on an amount or an address, the zero branch of a guard, not a balance equality an attacker can break by a wei | `LoanDust.t.sol`, `RouterDonation.t.sol` |
| `divide-before-multiply` (Medium) | `TickMath.getSqrtRatioAtTick`, `SlipstreamLpVenue._floorTo` / `_ceilTo` | **By design** — Uniswap's canonical tick math, and tick rounding to a spacing, which is integer division on purpose | `PythOracleAdapter.t.sol` (TickMath vectors), `SlipstreamLpVenue.t.sol` |
| `uninitialized-local` (Medium), 13 findings | counters and flags (`nFailed`, `kept`, `k`, `alive`, …) | **Cosmetic** — Solidity zero-initialises locals; every one is read as 0 / false on purpose | — |
| `unused-return` (Medium) | Aave tuple reads, `Clones.cloneDeterministic`, `POOL.swap`, `initialize` | **By design** — destructured tuples take the field they want; the clone's address is the CREATE2 prediction (`accountOf`, `Account.t.sol`); `POOL.swap`'s return is deliberately ignored in favour of the balance delta (W3-LOW-6) | `Account.t.sol`, `SwapAdapterFloor.t.sol` |
| `missing-zero-address-validation` (Low) | `OilskinAccount` / `OilskinAccountFactory` constructors (`factory`, `permit2`), `SnuggleLpVenue.dustFloor(token)` | **Covered elsewhere** — the deploy scripts refuse any dependency without code (`NoCode`), which is stricter than a zero check; `dustFloor` is a view | `Deploy.t.sol`, `DeploySepolia.t.sol` |
| `calls-inside-a-loop`, `low-level-calls`, `return-bomb`, `assembly-usage`, `block-timestamp`, `cyclomatic-complexity`, naming, `too-many-digits`, `cache-array-length`, `redundant-statements` (Low / Info / Optimisation) | the account's batch executor, the enumeration, deadlines and periods, the tick math | **By design** or style | — |

What this does NOT say: that halmos is clean — it has not run yet (the job stops at the first failing
step, and Aderyn was that step until the triage below). Its result is the next entry.

## Aderyn 0.6.8 — first run (CI on `7b5f72a`, 2026-09-12): four Highs, all triaged

Aderyn ran for the first time once Slither passed: **4 High, 15 Low** (the report is the run's
`static-analysis-reports` artifact, `aderyn.json`). Each High is suppressed at its site with an
`// aderyn-ignore-next-line(<detector>)` that names the reason; none is a code change.

| Detector (High) | Where | Verdict | Why |
|---|---|---|---|
| `contract-locks-ether` | `OilskinAccountFactory` | **False positive** | the factory's one payable door forwards `msg.value` in full to the new account (`initialize{value: msg.value}` / `execBatchFromFactory{value: msg.value}`); it has no `receive` or `fallback` and holds nothing (`Account.t.sol`, `audit-regressions/GrantEscape.t.sol` exercise the value path) |
| `eth-send-unchecked-address` | `PythOracleAdapter.refresh` — the refund `msg.sender.call{value: msg.value - fee}` | **By design** | the refund goes to whoever paid the update fee; there is no other recipient to check against (`PythOracleAdapter.t.sol` `refresh` tests) |
| `reentrancy-state-change` (14 instances) | the constructors of `PythOracleAdapter`, `SlipstreamPoolSwapAdapter`, `MorphoBlueVenue`, `SlipstreamLpVenue` (reads of `token0/1`, `decimals`, `nft`, `gauge`, `voter`, `market`), and `CollateralRegistry.register` (`decimals()` before the write) | **By design** | a contract under construction has no code, so nothing can re-enter it; `register` is owner-only and its only external calls are views on the token and venue the owner is registering (`CollateralRegistry.t.sol`, `audit-regressions/VenueSwitch.t.sol`) |
| `unsafe-casting` | `SlipstreamLpVenue._swap` — `uint16(tol)` | **Cannot truncate** | `toRatioToleranceBps` returns at most `BPS / 2` and then caps at `SWAP.MAX_SLIPPAGE_BPS()`, itself a `uint16` (`SlipstreamLpVenue.t.sol` W3-LOW-2 tests) |

The 15 Lows (centralization risk on the owner doors, literals instead of constants, PUSH0, unchecked
returns on the tuple reads, unsafe-ERC20 on the account's raw calls, unspecific pragma, …) are the
same patterns Slither's Lows describe above; they do not fail the job and are not re-triaged here.

## Halmos — first runs (2026-09-12): the account's grant properties hold; the harness had never executed

Halmos reached its first step once Aderyn passed (`b933864`) and the runner survived the compile
(`73598d9`), and failed **in `setUp()`**, before any property: `Unsupported cheat code:
deployCode(string,bytes)`. Not a property violation — Foundry 1.8 turns `dynamic_test_linking` on by
default, which compiles every `new X(...)` inside a test contract into `vm.deployCode("X", args)`
(the `VmContractHelper*` artifacts), and halmos has no such cheat. Bisected locally with three
throw-away harnesses (a bare mock deploys; `new OilskinAccount(...)` does not) and with the harness
frame's own call stack in halmos. Fix: `[profile.halmos] dynamic_test_linking = false`
(`contracts/foundry.toml`) — the harnesses compile with plain CREATE, nothing else changes.

| Harness | Properties | Local result (halmos 0.3.3, `--loop 4`) |
|---|---|---|
| `AccountGrantHalmos` (`test/halmos/AccountGrant.halmos.t.sol`) | an ungranted (target, selector) always reverts; a recognised token mover is charged exactly its calldata amount and never past the budget; the movers the parser cannot budget always revert; a non-mover is never charged | **4 passed / 0 failed** — 4 + 3 + 6 + 20 paths, 0.9 s |
| `RouterBalanceHalmos` (bounded `--loop 3`, `continue-on-error` in CI) | the router's balance is unchanged across `unwind` | **1 passed / 0 failed** — 293 paths, 33 s (`--solver-timeout-assertion 60000`) |

What this proves: the four account properties slice H wrote down hold for every symbolic input
halmos explores at loop bound 4 — the grant budget cannot be exceeded through any calldata shape the
parser recognises, and anything it cannot budget is refused — and that `unwind` leaves the router's
balance unchanged on every path halmos explores at loop bound 3. What it does not prove: anything
about deeper loops, or about the venues and adapters the router calls (mocked in the harness).

## Keeper valuation — CI's one red on `51273f5` (2026-09-12): a property test's premise, not the evaluator

**What CI showed.** The `agent` job on `51273f5` failed one subtest of `agent/test/valuation.test.ts`,
"UNKNOWN is sticky: adding poison never turns UNKNOWN into OK or NO_DEBT", with fast-check's shrunk
counterexample (seed `-1022574623`, path `52:2:3:2:2:2`): one USDC row, `aTokenBalance` 0, `debt` 1
base unit, not used as collateral, prices consistent; poisons `[hfMax, hfZero]`. The next push
(`32ca680`, same keeper code) was green, so the search simply had not drawn it before — a property
that fails on 1 of ~1,000 draws is a real counterexample, not noise. Replayed deterministically on the
founder's Mac (`fc.configureGlobal({ seed, path })` through `node --import`): "Property failed after
1 tests", the same counterexample.

**Severity.** Info. The evaluator (`agent/src/engine/valuation.ts`) is right at every step; the
property's premise is wrong for three of its poisons.

**The sequence.** `consistent(rows)` builds the shape Aave reports for a one-unit USDC residual with
no collateral: `healthFactorWad` 0, `totalDebtBase` 100 (1 unit × $1.00 in 8-dp base), and the
evaluator says **NO_DEBT** (slice C's dust rule: a residual at or below `LOAN_DUST_UNITS` is
rounding, nothing to protect). `hfMax` rewrites the pool HF to `MAX_UINT256` → **UNKNOWN** (G4: dust
debt but the pool claims an infinite HF — Aave never reports that with non-zero debt). `hfZero`
rewrites the same field to 0 → the snapshot is byte-for-byte the consistent shape again → **NO_DEBT**.
The property asserted UNKNOWN. But `hfMax`, `hfZero` and `hfScale` do not add a poison to the one
before them: they replace it, because all three write `healthFactorWad`. The evaluator judges the
snapshot in front of it, never a history, which is the property the monitor actually relies on.

**Fix (test only, this commit).** In the sticky property a HF poison that follows an earlier HF poison
is a *replacement*: it must still never turn UNKNOWN into OK, and every other poison, and the first HF
poison, must keep UNKNOWN as UNKNOWN. A poison that could not apply (no target row) no longer counts
as one. Checked: the fixed property passes at the CI seed and path (`--test-name-pattern`, 1 / 1), five
unseeded runs of the file are green, and the agent suite is **269 / 269** with `verify-abi` 113 / 113
and `verify-solana-idl` 77 / 77. Every other poison pair was walked for the same overwrite shape: the
Chainlink poisons on one row stay poisoned in any order (a missing feed is a G2 reason of its own —
`usableFeedPrice8`), the scale poisons compound, `ltZero` / `aavePriceZero` are idempotent.

**Regression.** The property itself, now with the seed recorded in its comment so the case can be
replayed by hand: `node --import <file that calls fc.configureGlobal({ seed: -1022574623, path:
"52:2:3:2:2:2" })> --test --test-name-pattern "UNKNOWN is sticky" agent/dist/test/valuation.test.js`.
