# Request for proposal — security audit, Oilskin **Base module** (EVM)

Prepared 2026-09-13. Everything below is measured from the tree at the hash named in §1, not estimated.
Wherever a number is the founder's to decide rather than the code's to report, it says so and is left blank —
please quote against the ranges in §6 and we will fix them before kickoff.

Abbreviations: LOC = lines of code; HF = health factor; LTV = loan-to-value; LP = liquidity provision;
CCTP = Circle's Cross-Chain Transfer Protocol; RFP = request for proposal.

## 1 · What you would audit, and at which commit

**Repository:** `mbrobert/ZEC-Defi-App` (private; read access provided on NDA).
**Measured at:** `18c86843d7d08b533ebee0591200d51b8b720991`, 2026-09-13 (re-measured; the first cut of this package was
`e1d57a49fde396a1c56d186af7ebc2bf90e521b3` the same day — see the delta note below §1's table).
**Audit target:** the freeze commit, which will be **tagged `beta-audit-1`** and is expected on **2026-12-11**.
This package is an inquiry against today's tree so that scope and price can be agreed before the freeze; the
scope has been stable in shape since 2026-09-06 and we will send a diff, not a new package, if it moves.

| Area | Size | Notes |
|---|---|---|
| `contracts/src` | **6,446 lines**, 32 Solidity files | solc 0.8.24, via-IR, EVM cancun. The whole on-chain surface |
| `contracts/test` | 15,598 lines | 39 suites: unit, audit-regression, invariant, fork, halmos |
| `agent/src` (off-chain keeper) | 11,070 lines TypeScript | In scope for *what it is permitted to do*, not as application code — see §3 |

Nothing has ever been deployed. There is no upgrade proxy, no admin key on the account, and no user funds at
risk today.

**What moved since the first cut of this package (`e1d57a4` → the hash above), all on 2026-09-13.** The shape
of the scope did not change; three things were added or fixed, and each is listed here so the diff can be read
rather than re-derived:

1. **`contracts/src/oracle/ChainlinkOracleAdapter.sol` is new** (+180 lines, +1 interface) — cbZEC priced from
   the Chainlink ZEC/USD feed exclusively, immutable, no admin, failing closed. It is **not deployed by any
   script** and its `maxAge` constructor parameter is deliberately unchosen (`VERIFIED-BASE-FACTS.md`
   Addendum 17 says why: the feed publishes on a 0.5 % deviation trigger and showed no heartbeat in 10.6 days,
   so no cadence measurement can bound it). Audit it as code; do not expect a deployment.
2. **`StrategyRouter` and `OilskinAccount` changed** for decision D9: an owner action that moves debt or
   collateral re-records the position's entry health factor, and the keeper's protective action must not.
   `OilskinAccount.keeperActor()` is new and exposes the transient actor slot so the router can tell them
   apart. **This is a trust-boundary change and worth your attention** — `AUDIT-2026-09-13.md` Part 3,
   finding LADDER-1, and `contracts/test/audit-regressions/EntryHfRerecord.t.sol`.
3. **The keeper's ladder is capped above an entry health factor of 2.00** (D10, LADDER-2) so it cannot repay
   or de-risk a position far clear of liquidation. Off-chain plus `packages/shared`; no Solidity change.

**Not in this RFP:** the Solana module (`solana/`, an Anchor program) is the subject of a parallel RFP. If you
audit both chains, say so in §6 question 5 — we would prefer one firm and one report if the calendar allows.

## 2 · What to read, in this order

1. `docs/AUDIT-SCOPE.md` — file-by-file scope, the invariants already asserted, and an explicit
   "not verified" list.
2. `CLAUDE.md` and `docs/BUILD-PLAN-2026-09-12.md` — what the product is and the decisions behind it.
3. `docs/RISKS.md` — the risk register, written for users rather than for us.
4. `docs/VERIFIED-BASE-FACTS.md` — every external address and parameter, read from chain and dated. No
   constant in this codebase is typed from memory; this document is why.
5. `docs/CROSSCHAIN-LOOP-2026-09-12.md` and `docs/CROSSCHAIN-RUNBOOK-2026-09-13.md` — the cross-chain design
   and its failure runbook.
6. Prior internal audits, with fixes and regression tests: `AUDIT-2026-09-06.md`, `AUDIT-2026-09-07.md`,
   `AUDIT-2026-09-12.md`, `AUDIT-2026-09-13.md`.

## 3 · The trust model in one page

- **The user's account** (`OilskinAccount`, an EIP-1167 clone) holds every position. Its owner is the user's
  wallet, set once and immutable. It cannot be upgraded and has no admin.
- **Peripherals are opt-in per call.** A call grants the target no rights over the account unless the caller
  sets `Call.callback`; only then may that target instruct the account back. Rights are not transitive.
- **The keeper** may invoke only `(target, selector)` pairs the owner granted, bounded by per-period token
  budgets, an expiry, and a revocation the owner can fire at any time. It never receives user funds. Two
  grants exist in the product: `StrategyRouter.unwind` and, for a cross-chain account,
  `StrategyRouter.closeLpAndBurn`.
- **The registry owner** is a timelocked role that can register assets and point them at venues. It is an
  owner and our copy says so; it cannot move funds.
- **Third parties we depend on and do not control:** Aave v3, Morpho Blue, Aerodrome and the MaxFi/Snuggle
  engine, Chainlink, Circle (both as USDC issuer and as the CCTP attester set).

## 4 · Invariants we already assert, and the ones we want attacked

Asserted in the suite, with the test paths in `docs/AUDIT-SCOPE.md`:

- The router's balance of **every** token it touches is unchanged across every entry point (a delta, not a
  zero — donations are inert).
- No borrow can open below the registry's entry HF floor; the **venue** enforces it, so no entry point can
  skip it.
- A keeper plan uses exactly one root selector, and it is one the user granted.
- Every close carries a price band, every swap a quote-derived floor, and a quote must agree with the band.
- `closeLpAndBurn` burns only to the recipient the owner recorded, never a parameter of the caller's.

**Please attack these specifically:**

1. Any path where a keeper, inside a legitimately signed grant, moves more value than the grant's budgets
   imply — including through a venue the budget parser cannot see.
2. Any path where the router ends a transaction holding a token, or where a donation can brick an entry point.
3. The multi-venue exit: an account with books on two venues, one disabled, one paused, mid-migration.
4. The cross-chain burn: anything that lets USDC leave Base to a destination the owner did not record.
5. The entry-HF floor under rounding — the offered LTV is `floor(LT × 100 ÷ floor)` and lands within 10.6
   points of Aave's own cap on cbBTC.
6. Re-entrancy across the account → peripheral → venue → account path, which is the one place we re-enter.

## 5 · Launch parameters (decided by the founder, 2026-09-13)

| Parameter | Value |
|---|---|
| Deposit cap per user | **$25,000** |
| Allowlist at launch | **25 addresses** |
| **Day-one funds at risk** | **$250,000** |
| Chains at launch | Base and Solana, together |

**The total is the binding number, and it is deliberately below the other two.** Twenty-five addresses at a
$25,000 cap would allow $625,000; the launch stops at $250,000, so **$250,000** is the figure to size this
engagement and any bug bounty against. The cap and the allowlist bound how that total can be distributed — no
single depositor can be more than a tenth of the book.

**For scale on this chain:** at the offered loan-to-value (62.40 % on cbBTC, being the liquidation threshold
divided by the 1.25 entry floor), $250,000 of collateral borrows at most about **$156,000** of USDC. Aave's
Base USDC reserve held **182,806,571 supplied against 158,038,067 borrowed** when it was read at block
51,227,701 (`VERIFIED-BASE-FACTS.md` Addendum 13), leaving about **24.77 million USDC** available to borrow.
The product's whole day-one book is therefore well under 1 % of it: Aave's depth is not a constraint here, our
own cap is.

## 6 · The six questions (asked identically of every firm, so that quotes compare)

1. **Earliest kickoff date** you could commit to, assuming a signed engagement within two weeks of your reply.
2. **Review length** in calendar weeks for the scope in §1, and how many engineers.
3. **Fix-review window**: is a re-review of our fixes included, and for how long after delivery?
4. **Price**, as a range against the scope in §1, and what would move it.
5. Do you audit **Solana (Anchor/Rust)** to the same standard? If so, quote both modules together as well as
   separately.
6. Will you **publish the report**, and may we publish it before launch?

## 7 · Practical notes

- Every suite is green at the hash in §1: contracts **433 passed / 0 failed / 13 skipped** across 39 suites;
  the fork suite **13 / 13** against Base at block 51,222,568; the ABI seam **444** selectors, topics and
  errors matching the compiled artefacts; shared **99**; keeper **316**; yield **178**; web **199** plus
  Playwright. CI runs every one of these on each push, plus Slither, Aderyn and halmos.
- `SETUP.md` builds the tree from clean in one page. Libraries are not vendored; `forge build` needs the two
  cloned dependencies it names.
- We run Slither, Aderyn and halmos in CI on every changed Solidity file and will hand you those reports.
- **Contact:** the founder, `founder@joule-solutions.com`.
